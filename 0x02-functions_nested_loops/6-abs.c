#include "main.h"
/**
 * _abs - absolute value of an integer
 *
 * @v: The int checked
 *
 * Return: The absolut value of v
 */
int _abs(int v)
{
	if (v >= 0)
	{
		return (v);
	}
	else
	{
		return (-v);
	}
}
