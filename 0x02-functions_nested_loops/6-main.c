#include "main.h"
/**
 * _abs - absolute value of an integer
 *
 * @v: The int checked
 *
 * Return: The absolut value of v
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
